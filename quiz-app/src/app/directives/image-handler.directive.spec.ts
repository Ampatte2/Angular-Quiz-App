import { ImageHandlerDirective } from './image-handler.directive';

describe('ImageHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
